---

# vars file for nodejs

_nodejs_path:
  tmp: "/var/tmp"

_nodejs_url:
  download: "https://nodejs.org/dist"

_nodejs_name_targz: "node-v{{nodejs_version}}-linux-x64.tar.gz"

_nodejs_version_download: "{{ _nodejs_url.download }}/v{{ nodejs_version }}/{{ _nodejs_name_targz }}"

